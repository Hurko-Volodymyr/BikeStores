version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    depends_on:
      - db
  db:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=MyStrongPassword123
      - MYSQL_DATABASE=bikestores
    ports:
      - 3306:3306
